from collections import Counter
n = int(input())
hcolor = []
gcolor = []
for i in range(n):
    h, g = map(int, input().split())
    hcolor.append(h)
    gcolor.append(g)
    hcount = Counter(hcolor)
    ans = 0
for g in gcolor:
    ans += hcount[g]
print(ans)
